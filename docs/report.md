# Ход работы и отладка

## Ход работы

1. Создан репозиторий и структура проекта.
2. Зафиксированы версии зависимостей в `requirements.txt`.
3. Настроен MkDocs с темой Material.
4. Реализован `scripts/benchmark.py`, который реально измеряет время выполнения детерминированной CPU-нагрузки.
5. Бенчмарк выполнен для пяти размеров входа, по 7 измеряемых повторов после прогрева; сырые результаты сохранены в `data/measurements.csv`, окружение — в `data/benchmark_metadata.json`.
6. Реализован `scripts/generate.py`, который проверяет checksum, вычисляет среднее и стандартное отклонение, формирует таблицу и график.
7. В генератор добавлено кэширование по содержимому данных, метаданных, кода и зависимостей.
8. Добавлена строгая сборка `mkdocs build --strict`.
9. Настроен GitHub Actions для проверки, сборки и публикации GitHub Pages.
10. После публикации добавлен healthcheck главной страницы, страницы P3 и PNG-графика.

## Происхождение исходных данных

Исходные значения времени не задаются вручную. Скрипт `scripts/benchmark.py` выполняет одну и ту же O(n)-нагрузку:

```python
total += (i * i + 3 * i) % 97
```

для 100 000, 250 000, 500 000, 1 000 000 и 2 000 000 итераций. Для каждого размера выполняется один прогревочный запуск и 7 измеряемых повторов. Используется `time.perf_counter_ns()`. Для каждого запуска сохраняется checksum, поэтому можно проверить, что повторы выполняли одинаковую вычислительную работу.

Точный Python, ОС, архитектура, дата измерения, число повторов и таймер сохранены в `data/benchmark_metadata.json`.

## Измерения CI/CD

Контрольные измерения ниже получены из реальных GitHub Actions запусков и Pages-артефакта. После окончательного обновления экспериментальных данных значения размера итоговой страницы могут немного измениться.

### Время

| Этап | Результат |
|---|---:|
| Генерация без кэша | 0.1450 с |
| Генерация с кэшем | 0.0005 с |
| Полный build-job | 31 с |
| Deploy-job | 8 с |
| Шаг `actions/deploy-pages` | 5 с |

Время полного build-job включает установку зависимостей; в контрольном запуске именно она заняла основную часть времени.

### Размер результата

| Артефакт | Размер |
|---|---:|
| Главная HTML-страница | 18 915 байт (~18.5 KiB) |
| HTML-страница P3 | 21 909 байт (~21.4 KiB) |
| График `experiment.png` | 44 760 байт (~43.7 KiB) |
| Весь статический сайт без сжатия | 2 732 152 байт (~2.61 MiB) |
| Pages artifact | 648 432 байт (~633 KiB) |

## Отладка

### Ошибка 1. Репозиторий не имел ни одного коммита

**Ошибка:** запрос содержимого пустого репозитория через GitHub API возвращал `404 This repository is empty`.

**Гипотеза:** default branch указана как `master`, но Git-ссылка ещё не существует без первого коммита.

**Проверка:** метаданные репозитория показали размер 0 и отсутствие содержимого.

**Решение:** создан первый файл `README.md`, после чего ветка `master` была инициализирована.

### Ошибка 2. Строгая сборка обнаружила битую ссылку

**Ошибка:** `WARNING - Doc file 'index.md' contains a link 'missing.md', but the target is not found among documentation files.` Затем MkDocs завершился сообщением `Aborted with 1 warnings in strict mode!`.

**Гипотеза:** ссылка ведёт на файл, которого нет в `docs/`, а режим `--strict` превращает предупреждение в ошибку.

**Проверка:** в логе GitHub Actions шаг `Strict MkDocs build` завершился с exit code 1, остальные предыдущие шаги были успешными.

**Решение:** удалена тестовая ссылка на `missing.md`. Это подтверждает, что strict-сборка действительно блокирует публикацию сайта с битой внутренней ссылкой.

### Ошибка 3. GitHub Pages не был включён в настройках репозитория

**Ошибка:** build-job завершился успешно, но deploy-job получил `HttpError: Not Found` и сообщение `Failed to create deployment (status: 404) ... Ensure GitHub Pages has been enabled`.

**Гипотеза:** workflow корректно создаёт Pages-артефакт, но для нового репозитория ещё не выбран источник публикации GitHub Actions.

**Проверка:** шаги генерации, `mkdocs build --strict` и `Upload Pages artifact` завершились успешно; ошибка возникла только в `actions/deploy-pages`.

**Решение:** в настройках репозитория выбран `Settings → Pages → Source: GitHub Actions`. После переключения повторный запуск успешно прошёл целиком: `build` и `deploy` завершились со статусом `success`, а сайт опубликован по адресу `https://dmitryledentsov.github.io/web-solutions-lab-1/`.

## Вывод

Для небольших исследовательских проектов подходит связка Python + MkDocs: получение исходных измерений вынесено в отдельный воспроизводимый скрипт, анализ и построение артефактов выполняются перед сборкой сайта, а MkDocs отвечает за публикацию Markdown и статики. Если вычисления становятся тяжёлыми или требуют GPU, их целесообразно выполнять отдельно и публиковать заранее рассчитанные артефакты с метаданными версии.

## Скриншоты для отчёта

В итоговый отчёт необходимо вставить:

1. успешный финальный workflow, где видны зелёные jobs `build` и `deploy`;
2. намеренно проваленный запуск со step `Strict MkDocs build`;
3. страницу P3 с таблицей и графиком уже после перехода на реальные измерения.
